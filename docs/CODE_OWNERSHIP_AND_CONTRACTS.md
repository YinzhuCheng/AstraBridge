# Code Ownership And Contract Boundaries

Last updated: 2026-07-17

## Purpose

This document names the canonical owner for high-risk AstraBridge contracts. It prevents an implementation detail, compatibility representation, or UI projection from becoming a second business contract.

Run the boundary audit before changing any owner or bridge:

```powershell
python scripts/contract_boundary_audit.py
```

The quick local gate runs the same audit.

## Current Boundaries

| Concern | Canonical owner | Compatibility or projection boundary | Required validation |
| --- | --- | --- | --- |
| Provider metadata, capability policy, reasoning and safety defaults | `astrabridge_sidecar.providers.profile` and `providers.registry` | `router_config_service.py` persists user overrides and derives runtime profiles; it must not create a second provider-family policy. | Provider catalog/compatibility tests and transport registry audit. |
| Provider wire selection | `astrabridge_sidecar.providers.transports` | `router_service.py` selects a registered transport, performs HTTP/SSE plumbing, and must not contain provider-specific transport implementations. | `test_router_transport_registry.py` plus retired-symbol governance gate. |
| Provider runtime client lanes and lifecycle leases | `astrabridge_sidecar.runtime_client_pool.RuntimeClientPool` with `runtime_service.py` as the integration owner | Each lane owns its app-server client, private environment, Codex home, concurrency lease, and restart/reap policy; `RuntimeService._client` is only a compatibility projection. | Barrier-controlled lane tests, runtime service regressions, pool snapshot secret scan, and process audit. |
| Normalized provider result | `astrabridge_sidecar.providers.ir` | Individual transports normalize upstream wire payloads into `NormalizedResponse`; router streaming projects that result for the client. | Provider compatibility, tool-call and router transport tests. |
| Versioned cross-provider protocol schemas and generated types | `astrabridge_sidecar.protocol.schema.v1/protocol.json` with `scripts/generate_protocol_types.py` | `agent_orchestration_contract.py`, `task_graph_contract.py`, and inline Desktop types are compatibility bridges; generated AstraBridge types live under `protocol/generated` and `src/astrabridge_protocol/generated`, separate from Codex/app-server generated types. | Protocol schema fixtures, Python/TypeScript generation freshness, migration tests, and this boundary audit. |
| Canonical protocol durable-write boundary and runtime vocabularies | `astrabridge_sidecar.protocol.persistence` plus `astrabridge_sidecar.durable_run_store.DurableRunEventStore` | `task_service.py` and `runtime_service.py` may still construct legacy task-graph run refs and timeline summaries, but persisted envelopes, events, content parts, artifact refs, and protocol vocabulary snapshots must pass through the shared protocol write-boundary normalizer rather than inventing schema-external event types or path-based artifact identities. | Protocol persistence unit tests, durable run-store regressions, generated Desktop protocol tests, quick gate, and contract boundary audit. |
| Agent Envelope, delivery events, and artifact references | `astrabridge_sidecar.protocol` | Existing task-graph input/output envelopes and `agent_orchestration_contract.py` are compatibility projections; no provider transcript or UI summary may become a second envelope. | Envelope validation, delivery idempotency, artifact URI safety, and cross-provider handoff tests. |
| MCP protocol core and broker boundary | `astrabridge_sidecar.mcp_server_core` plus `astrabridge_sidecar.mcp_broker_service.McpBrokerService` | `mcp_config_service.py` owns MCP configuration/presets; named MCP servers such as capability/web/yunwu/probe are thin tool adapters over the shared core only. `server.py`, `runtime_service.py`, and `web_tool_service.py` must route normal capability invocation through the broker rather than calling capability/web/yunwu implementations directly. Capability implementations remain in `capabilities/`. Remote `streamable_http` MCP authorization discovery, protected-resource least-scope token checks, session pooling/recovery, and durable external-operation bridging must stay inside the shared broker instead of being reimplemented in route handlers, web lane code, or individual capability adapters. | MCP conformance, broker routing, policy/approval, shared-core parity, remote protected-resource authorization, durable bridge recovery, HTTP/runtime metadata propagation, and direct-bypass boundary tests. |
| Per-node MCP tool/resource least-privilege policy | `astrabridge_sidecar.mcp_node_policy` | `agent_orchestration_contract.py` validates authoring fields, `agent_orchestration_compiler.py` snapshots planned node policy, `runtime_service.py` filters exposed dynamic tools and carries run/node/attempt policy context, and `mcp_broker_service.py` re-authorizes dispatch. None of those bridges may invent a second policy format or silently widen tool/resource access. | Node-policy allow/deny/ask/reuse/budget/resource escape fixtures, graph-worker snapshot drift tests, broker pre-dispatch denial tests, and contract boundary audit. |
| Typed multimodal MCP results and workspace-safe artifact projection | `astrabridge_sidecar.multimodal_result_envelope` | Capability adapters, Yunwu/web MCP adapters, capability artifact snapshots, Desktop capability rendering, and remote MCP broker result passthrough may project typed result summaries, but they must reuse the shared workspace-safe artifact URI, digest, size, lineage, `ContentPart`, and inline-externalization rules rather than inventing per-surface result shapes or flattening remote multimodal results into plain text. | Image/vision/speech/web typed-result fixtures, remote typed-result passthrough checks, artifact URI escape rejection, base64 externalization, Desktop capability panel component tests, and preserved visual QA screenshots. |
| Desktop-Sidecar host supervision, readiness, launch ownership, and run reattachment handshake | `apps/astrabridge-desktop/src-tauri/src/sidecar_supervision.rs` plus `astrabridge_sidecar.server.py` for `/readyz` and graceful shutdown | `main.rs` only wires the supervisor into Tauri state and `src/api.ts` only resolves the current bound sidecar URL through that owner. No Desktop/UI module may reintroduce a fixed port, kill-by-port behavior, or an alternate launch-record/readyz contract. | Rust supervision restart/share/churn tests, sidecar readyz/shutdown route tests, Desktop API tests, process audit, and contract boundary audit. |
| Cross-layer trace lineage, reliability metrics/SLOs, redacted diagnostics, and runtime support bundles | `astrabridge_sidecar.runtime_observability` | `runtime_service.py` records/enriches runtime events and host lineage into the canonical observability event stream, `runtime_supervisor_service.py` projects one summarized view for Desktop and persists the redacted runtime support bundle, and Desktop status evidence may render those summaries without inventing a second diagnostic state machine or support-bundle exporter. | Runtime observability unit tests, runtime-supervisor status tests, support-bundle secret-scan checks, Desktop build/tests, secret-redaction checks, and contract boundary audit. |
| Deterministic fault injection and runtime stability release gate (including the release-consumable fault matrix) | `astrabridge_sidecar.runtime_stability_gate` | `scripts/run_runtime_stability_gate.py` is the only public CLI wrapper for the release gate, and `scripts/run_local_gate.py` may invoke only the fast mode as a normal-gate projection. No other script may define a parallel runtime-stability suite matrix, release fault matrix, artifact layout, or secret-scan rule for `PRIVATE/runtime-stability/**`. | Runtime stability gate tests, required fault-matrix field checks, targeted scheduler/MCP/provider/host suites, runtime-stability artifact secret scan, process inventory capture, and contract boundary audit. |
| Fail-closed promotion verdicts and CI entry points | `astrabridge_sidecar.promotion_gate` | `scripts/run_promotion_gate.py` is the only public CLI wrapper for PR/nightly/release promotion verdicts. `.github/workflows/*.yml` may install dependencies and upload artifacts, but they must call the wrapper instead of duplicating suite lists or inventing a second promotable-summary contract. | Promotion-gate unit tests, workflow marker audit, quick gate, and contract boundary audit. |
| Release identity, clean staging allowlist, and readiness verification | `astrabridge_sidecar.release_identity` plus `release/astrabridge-release-identity.json` | `scripts/run_release_readiness_gate.py` is the only public readiness wrapper. Static package metadata files such as `package.json`, `pyproject.toml`, `Cargo.toml`, `tauri.conf.json`, MCP server metadata, and protocol compatibility files are consumers that must match the canonical release identity instead of defining independent release versions. | Release-identity unit tests, readiness gate evidence, drift-failure fixtures, quick gate, and contract boundary audit. |
| Persisted project task graph and run history | `task_graph_contract.py` | `task_service.py` owns storage/API lifecycle and validates all persisted graph/run payloads. | `test_task_graph_contract.py`, task-graph API/runtime tests, and contract boundary audit. |
| Task-graph mutation, task-scoped import/export lifecycle, and graph-edit primitives | `astrabridge_sidecar.task_graph_mutation_service` | `task_service.py` remains the task-scoped storage/API, revision-token, source-ownership, and snapshot bridge, but graph import/export transforms, node/edge mutation primitives, and persist-preparation must route through the shared mutation service instead of growing parallel edit logic inside `task_service.py`, Desktop routes, or server handlers. | Task-graph API characterization tests, mutation-service delegation checks, import/export round-trip tests, revision-conflict tests, and contract boundary audit. |
| Live graph-run queue admission, scheduler receipt/status projection, and cancellation coordination | `astrabridge_sidecar.runtime_graph_run_dispatch_service` | `runtime_service.py` remains the runtime/server lifecycle bridge, scheduler callback owner, provider-turn execution loop, and interruption/reconciliation bridge, but queue admission, run-status projection, and live cancellation coordination must route through the shared dispatch service instead of growing parallel logic inside `runtime_service.py`, API handlers, or scheduler wrappers. | Graph-scheduler/runtime characterization tests, live cancel/recovery tests, HTTP task-graph route tests, and contract boundary audit. |
| Durable graph run state, scheduler commands, and ordered events | `astrabridge_sidecar.durable_run_store.DurableRunEventStore` plus `astrabridge_sidecar.graph_scheduler.DurableGraphScheduler` | Workspace-local `.astrabridge/durable_runs.sqlite3` is the transactional source of truth; `DurableGraphScheduler` owns admission/dispatch callbacks; `task_service.py` JSON, run manifests, diagnostics, and UI refs remain redacted compatibility projections/rebuildable exports. | Store migration, CAS/terminal-state, lease/recovery, idempotency, projection-rebuild, bounded scheduler, and request-disconnect tests. |
| Portable, typed agent orchestration graph | `agent_orchestration_contract.py` | `agent_orchestration_compiler.py` compiles canonical graphs. `lift_task_graph_to_agent_orchestration_graph` and `lower_agent_orchestration_graph_to_task_graph` are the only format bridge. | Agent orchestration contract/compiler/check tests and contract boundary audit. |
| External A2A gateway, Agent Card registry, trust policy, and conformance corpus | `astrabridge_sidecar.external_a2a_gateway` plus `astrabridge_sidecar.external_a2a_conformance` and `agent_orchestration_contract.py` / `agent_orchestration_compiler.py` for compile/admission-time resolution | External Agent Cards, task/message/part/artifact adapters, supported protocol-version window, transport/auth subset, trust levels, replay protection, signed-card verification policy, negotiation outcomes, and reusable negative conformance corpus are owned by the shared gateway modules. `agent_orchestration_contract.py` may validate `external_agent_card_registry` ownership and `a2a_card:` references, while `agent_orchestration_compiler.py` may snapshot resolved external cards into compiled-plan compatibility metadata. None of those bridges may move external A2A fields into the durable protocol schema or create a second registry, trust-policy owner, replay journal, or lifecycle state machine. | External A2A gateway/conformance unit tests, orchestration contract/check tests, quick gate, and contract boundary audit. |
| Canonical NodeType registry and compiled graph executable metadata (including the executor capability matrix) | `astrabridge_sidecar.node_type_registry` plus `agent_orchestration_compiler.py` for registry-resolved executor projection | `task_graph_contract.py`, `agent_orchestration_contract.py`, `task_service.py`, `runtime_service.py`, `server.py`, `src/api.ts`, and Desktop role/palette helpers are compatibility bridges or future consumers; they may not create a second node-type schema, alias table, executor matrix, or execution fingerprint source. | Registry snapshot/API tests, duplicate-registration startup tests, compiler fingerprint/executor tests, unknown-node preservation tests, live/fixture executor fail-close tests, Desktop build/typecheck, and contract boundary audit. |
| Loss-aware ComfyUI workflow import/export bridge | `astrabridge_sidecar.comfyui_workflow_adapter` | `task_service.py` owns task-scoped file I/O, lifecycle import/export, and task-graph overlay application; Desktop `src/api.ts` and `src/App.tsx` may prompt for paths, preview exports, and surface machine-readable loss reports, but they may not invent a second ComfyUI node map, adapter manifest, or loss-report schema. | ComfyUI adapter round-trip/loss tests, task-graph API import/export tests, workspace-safe artifact URI validation, and contract boundary audit. |
| Optional LangGraph StateGraph interop bridge | `astrabridge_sidecar.langgraph_stategraph_adapter` | `task_service.py` owns task-scoped file I/O, lifecycle import/export, and task-graph overlay application; Desktop `src/api.ts` and `src/App.tsx` may prompt for paths, preview exports, and surface adapter diagnostics, but they may not invent a second LangGraph node/edge map, compile-config schema, thread/checkpoint lineage mapping, or generated-code contract. | LangGraph adapter round-trip/codegen/loss tests, task-graph API import/export tests, no-dependency conformance, and contract boundary audit. |
| Final runtime rollout, migration, shadow comparison, rollback-readback, and release closure | `astrabridge_sidecar.runtime_rollout_gate` | `runtime_stability_gate.py` remains the owner for the nested deterministic suite matrix and release-consumable fault matrix; `durable_run_store.py` remains the owner for legacy import mechanics and durable state; `task_service.py` remains the owner for fixture-run execution. The rollout gate may orchestrate those owners and publish evidence, but it must project the shared fault matrix instead of defining a second rollout-closure matrix, shadow-comparison contract, or rollback-readback rule. | Runtime rollout gate tests, runtime stability release gate evidence, release fault-matrix projection checks, migration idempotency/shadow comparison/rollback-readback artifacts, desktop visual QA capture, and contract boundary audit. |
| Desktop task-graph workspace shell-state persistence and panel sizing | `apps/astrabridge-desktop/src/features/runtime/taskGraphWorkspacePersistence.ts` | `TaskGraphWorkspace.tsx` remains the graph editor/renderer bridge, but sidebar-width normalization, workspace localStorage keying, stored inspector workspace restore, and pending run-inspector reopen state must route through the shared persistence owner instead of growing ad hoc storage logic inside the component. | Desktop typecheck/build, `taskGraphWorkspacePersistence.test.ts`, and focused task-graph workspace UI tests. |
| Desktop App-level task-graph selection and run-monitoring projections | `apps/astrabridge-desktop/src/features/runtime/taskGraphAppState.ts` | `App.tsx` remains the top-level query/mutation wiring and dispatch-timeout bridge, but current-graph selection, per-graph snapshot/edit-history/command-log projection, optimistic versus authoritative run-ref derivation, and task-graph dataset payload assembly must route through the shared app-state owner instead of growing a second inline selector. | Desktop typecheck/build, `taskGraphAppState.test.ts`, and focused task-graph selection/run-ref tests. |
| Desktop task-graph inspector modal chrome and workspace switching presentation | `apps/astrabridge-desktop/src/features/runtime/TaskGraphInspectorModal.tsx` | `TaskGraphWorkspace.tsx` remains the graph editor, selection state, and editable inspector-content bridge, but inspector scrim/modal chrome, header subtitle, workspace tabs, and selection-mode chip presentation must route through the shared inspector-modal owner instead of growing another inline modal shell inside the workspace component. | Desktop typecheck/build, `TaskGraphInspectorModal.test.tsx`, and focused `TaskGraphWorkspace.test.tsx` inspector regressions. |
| Desktop graph rendering and edits | Desktop runtime graph components | Desktop may project and edit validated API payloads, but may not invent a different graph schema or bypass Sidecar validation. | Desktop typecheck/build and task-graph UI tests. |

## Task Graph Ownership Rules

- `.abproj` task state remains the persisted lifecycle contract. Its graph and run schema versions are declared in `task_graph_contract.py`.
- `astrabridge-agent-orchestration-graph-v1` is the portable, typed authoring and compilation contract. It carries ports, modality claims, prompts, isolation and handoff rules that the persisted task graph cannot fully express.
- `astrabridge_sidecar/task_graph_mutation_service.py` is the only owner for task-graph import/export transforms, node/edge mutation primitives, overlay application, and persist-preparation for graph edits. `task_service.py` remains the task-scoped revision, source-ownership, snapshot, and storage/API bridge, but it must not grow a second inlined graph-edit engine.
- `astrabridge_sidecar/runtime_graph_run_dispatch_service.py` is the only owner for live graph-run queue admission, scheduler receipt/status projection, and cancellation coordination. `runtime_service.py` remains the scheduler callback, runtime lifecycle, provider-turn execution, interruption, and reconciliation bridge, but it must not grow a second inlined queue/cancel coordination engine.
- `apps/astrabridge-desktop/src/features/runtime/taskGraphWorkspacePersistence.ts` is the only owner for Desktop task-graph workspace shell-state persistence, sidebar-width normalization, workspace localStorage keys, stored inspector-workspace restore, and pending run-inspector reopen markers. `TaskGraphWorkspace.tsx` remains the editor/rendering bridge, but it must not grow a second inline persistence contract for those fields.
- `apps/astrabridge-desktop/src/features/runtime/taskGraphAppState.ts` is the only owner for Desktop App-level current task-graph selection, per-graph snapshot/edit-history/command-log projection, optimistic versus authoritative run-ref derivation, and task-graph dataset payload assembly. `App.tsx` remains the top-level query/mutation wiring and dispatch-timeout bridge, but it must not recreate a second inline task-graph app-state selector for those fields.
- `apps/astrabridge-desktop/src/features/runtime/TaskGraphInspectorModal.tsx` is the only owner for Desktop task-graph inspector modal chrome, workspace-switch tabs, header subtitle rendering, and selection-mode chip presentation. `TaskGraphWorkspace.tsx` remains the graph editor, selection/run state owner, and editable inspector-body bridge, but it must not recreate a second inline inspector modal shell for those fields.
- Conversion is intentionally lossy in the canonical-to-persisted direction. New semantics belong in the orchestration contract and require an explicit lowering policy; no caller may copy fields ad hoc between the two dictionaries.
- `TaskService._sync_orchestration_graph_with_task_graph` is the only lifecycle synchronizer. It must preserve graph identity, topology and entry-node ownership. It is not a second validator.
- `apps/astrabridge-sidecar/astrabridge_sidecar/protocol/schema/v1/protocol.json` is the canonical cross-provider schema source. `scripts/generate_protocol_types.py --check` is the freshness gate; `protocol/compatibility.py` is the only legacy graph/artifact migration adapter.
- `apps/astrabridge-sidecar/astrabridge_sidecar/protocol/persistence.py` is the only owner for durable-write normalization of protocol events, envelopes, content parts, artifact refs, and runtime vocabulary snapshots. `durable_run_store.py` enforces that owner at the SQLite boundary; task/runtime services are callers only.
- `DurableRunEventStore` owns live run identity, state-version CAS, ordered events, node attempts, leases, inbox/outbox records, and external-operation lineage. Legacy task JSON and manifests are imported idempotently; active legacy runs become `needs_review` and are never resumed implicitly.
- `DurableGraphScheduler` owns the asynchronous receipt/worker seam. `RuntimeService.execute_task_graph_run` remains a synchronous compatibility adapter for explicit callers/tests; the normal HTTP route uses `queue_task_graph_run` and reads `graph_run_status` from the durable projection.
- Durable projections must be deterministic and rebuildable from the workspace-local store. No provider secret, bearer token, cookie, or external artifact path may be persisted in the store; legacy source files and private evidence remain untouched.

## Provider Ownership Rules

- A provider family is registered once in `providers/transports/__init__.py`. Router code resolves the class through `transport_class_for_profile(...)`.
- Provider-private request fields, response parsing and reasoning conventions stay in the owning transport subclass.
- Shared message conversion, tool projection, image handling, response summary/redaction and normalized result types stay in `providers/transports/base.py` and `providers/ir.py`.
- Historical inline adapters are archived evidence only. Their names are prohibited in current runtime code by `scripts/repo_governance_check.py`.

## Stability Plan Ownership Rules

- `PLAN/ASTRABRIDGE_PRODUCT_STABILITY_AND_INTEROPERABILITY_EXECUTION_PLAN.md` is the only active scheduler for multi-provider control, external A2A interoperability, shared MCP/runtime follow-ons, GUI/code orchestration parity, signed updates, and related release-gate work.
- `PLAN/ASTRABRIDGE_STABILITY_PROTOCOL_AND_AGENT_RUNTIME_EXECUTION_PLAN.md` is the completed implementation baseline; preserve and reuse its contracts and evidence rather than reopening its execution queue.
- Capability-specific adapter qualification remains independent, but any shared MCP transport, envelope, artifact, run-state, scheduler, A2A gateway, executor registry, updater, or promotion-gate change must be implemented through the active product-stability plan.
- `astrabridge_sidecar.protocol` owns cross-provider schemas; `astrabridge_sidecar.durable_run_store.DurableRunEventStore` owns durable run state. Existing graph, capability, and Desktop modules are bridges until a numbered migration step moves their consumers; they must not silently add parallel schema fields.
- `astrabridge_sidecar/protocol/persistence.py` is the only owner for schema-derived runtime event vocabularies and protocol durable-write normalization. Runtime/task code may prepare compatibility records, but schema-external event types and path-only artifact refs must not cross the durable SQLite boundary.
- `apps/astrabridge-desktop/src-tauri/src/sidecar_supervision.rs` is the only Desktop owner for sidecar launch records, readiness gating, bounded restart, circuit breaking, lease sharing, and ownership-verified shutdown. `astrabridge_sidecar/server.py` is the only sidecar owner for `/readyz` and graceful host shutdown. `main.rs` and `src/api.ts` are bridges only.
- `astrabridge_sidecar/runtime_observability.py` is the only owner for trace-context normalization, host-lineage ingestion, stable reliability metrics/SLO thresholds, OpenTelemetry mapping metadata, recent-diagnostic summaries, redacted runtime support-bundle schema/layout, and support-bundle secret-scan policy. `runtime_service.py` may enrich events before persistence, `runtime_supervisor_service.py` may summarize them for Desktop and persist the bundle, and Desktop may render those summaries, but none of those bridges may define a second observability schema, support-bundle exporter, or local diagnostic state machine.
- `astrabridge_sidecar/runtime_stability_gate.py` is the only owner for the deterministic runtime-stability suite matrix, the release-consumable fault matrix, the `PRIVATE/runtime-stability/<run-id>/` artifact layout, fixture evidence capture, and runtime-stability secret-scan policy. `scripts/run_runtime_stability_gate.py` is a wrapper only, and `scripts/run_local_gate.py` may only call the fast mode rather than defining a second suite list or release fault matrix.
- `astrabridge_sidecar/promotion_gate.py` is the only owner for fail-closed PR/nightly/release verdict semantics, required-status-path evaluation, commit/dirty/toolchain binding, gate-manifest generation, and nested summary/report digest binding. `scripts/run_promotion_gate.py` is a wrapper only, and `.github/workflows/*.yml` are entry points only.
- `astrabridge_sidecar/release_identity.py` plus `release/astrabridge-release-identity.json` are the only owners for release version identity, static metadata drift checks, clean staging allowlists, staged updater-manifest derivation, package inventory/hashes, dependency-lock digests, SBOM inputs, and readiness verification. `scripts/run_release_readiness_gate.py` is a wrapper only.
- `astrabridge_sidecar/node_type_registry.py` is the only owner for canonical node type specs, legacy kind alias resolution, registry fingerprints, executor capability matrices, registry API payloads, and task-graph/orchestration projection defaults for node types. This remains the canonical NodeType registry and compiled graph executable metadata owner. `task_graph_contract.py`, `agent_orchestration_contract.py`, `agent_orchestration_compiler.py`, `task_service.py`, `runtime_service.py`, `server.py`, and Desktop graph helpers may consume or project that registry, but none of them may define a second alias table, type list, executor availability table, or execution fingerprint source.
- `astrabridge_sidecar/comfyui_workflow_adapter.py` is the only owner for the supported ComfyUI save-workflow subset mapping, AstraBridge extension namespace, opaque unsupported-node preservation rules, machine-readable loss reports, and workspace-safe artifact URI validation during ComfyUI import/export. `task_service.py` may choose the adapter based on file content and apply task-graph overlays for UI editing, while Desktop may preview or display adapter diagnostics, but none of those bridges may define a second ComfyUI node map, alternate loss-report contract, or independent extension namespace.
- `astrabridge_sidecar/langgraph_stategraph_adapter.py` is the only owner for the supported LangGraph StateGraph manifest subset, compile-time checkpointer/interrupt/subgraph mapping, thread/checkpoint lineage projection, generated LangGraph integration code shape, and machine-readable loss reports for unsupported constructs. `task_service.py` may choose the adapter based on file content and apply task-graph overlays for UI editing, while Desktop may preview or display adapter diagnostics, but none of those bridges may define a second LangGraph node/edge map, alternate compile-config schema, or independent generated-code contract.
- `astrabridge_sidecar/external_a2a_gateway.py` plus `astrabridge_sidecar/external_a2a_conformance.py` are the only owners for the supported external A2A protocol-version window, Agent Card registry schema, digest pinning, trust levels, replay protection, signed-card verification rules, negotiation outcomes, supported interface/auth subset, reusable negative conformance corpus, external task lifecycle transition rules, and the bidirectional adapters between external A2A tasks/messages/parts/artifacts and AstraBridge `AgentTask`/`AgentEnvelope`/`ContentPart`/`ArtifactRef`. `agent_orchestration_contract.py` may validate `a2a_card:` references against the registry, and `agent_orchestration_compiler.py` may snapshot resolved external cards into compiled-plan compatibility metadata, but neither bridge may move external A2A fields into the durable protocol schema or create a parallel registry, trust-policy owner, replay journal, or lifecycle owner.
- `astrabridge_sidecar/runtime_rollout_gate.py` is the only owner for the final rollout feature-flag manifest, compatibility window, shadow-comparison state matrix, bounded dogfood migration copy, rollback-readback proof, and release-closure evidence bundle. `runtime_stability_gate.py` remains the owner for the nested deterministic runtime stability suite and release-consumable fault matrix; `durable_run_store.py` remains the owner for legacy import mechanics; and Desktop/browser capture scripts remain wrappers only. No other script or report may define a second release-closure matrix, release fault matrix, shadow-comparison rule, or rollback-readback policy for this plan.
- The new owner boundary does not authorize a wholesale rewrite. Each bridge must have a compatibility test, an explicit migration status, and a removal or sunset condition.

## Change Rules

1. Change the canonical owner first, then update bridges and projections.
2. Add a contract test that fails on the old field or route shape before accepting a migration.
3. Extend `scripts/contract_boundary_audit.py` when a new provider family, graph schema or bridge is introduced.
4. Keep a compatibility shim free of business logic. Record its replacement and sunset state in `docs/archive/LEGACY_COMPATIBILITY_SHIMS.md` and `docs/INTERFACE_GOVERNANCE.md`.
5. Do not use source-string replacement or unvalidated dictionary mutation for graph contract conversion.

## Current Automated Evidence

`scripts/contract_boundary_audit.py` validates every built-in persisted task-graph fixture and every built-in orchestration example through validation, conversion and compilation. It fails if graph identity, node/edge topology, entry-node ownership, schema versions or the provider transport registry drift.

The same audit also verifies that the stability plan, protocol package marker, durable run-store owner, MCP broker owner, capability-plan delegation, and this ownership table agree. Any stricter direct-bypass guard must first update the canonical owner and its compatibility boundary here.
