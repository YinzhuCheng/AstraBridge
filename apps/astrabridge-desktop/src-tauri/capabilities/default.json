{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AstraBridge main window with no direct shell or filesystem permissions.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}

